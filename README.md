# LexLoom
LexLoom is a universal syntax highlighting generator. It allows developers to define a custom programming language's grammar once using a clean, regex-free JSON file, and instantly compile it into native syntax highlighters for VS Code, Vim, and Notepad++
